server:
  port: 8080
  admin_key: "my-custom-key"
  rate_limit: 5
  audit_log: "custom-audit.log"

routes:
  - path: "/"
    target: "http://localhost:9000"

security:
  enable_xss: true
  enable_sqli: false
  enable_dlp: true
