server:
  port: 8080
  admin_key: "secret-sentinel-key"
  rate_limit: 10
  audit_log: "audit.log"

routes:
  - path: "/"
    targets:
      - "http://localhost:9000"
      - "http://localhost:9002"

security:
  enable_xss: true
  enable_sqli: true
  enable_dlp: true
